/**
 * TODO: Write tests for:
 * - conditional rendering
 * - correct list items rendering
 */
import React from 'react';
import * as enzyme from 'enzyme';

describe('CharactersList', () => {
  it('Should implement tests', () => (undefined));
});